eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
    instance:
      prefer-ip-address: true

ribbon:
  ConnectTimeout: 10000
  ReadTimeout: 50000
  eureka:
    enabled: true
